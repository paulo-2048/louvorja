{
  "name": "@louvorja/app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "lerna run build:dist --scope @louvorja/app-* && cp index.html dist/",
    "dev": "nodemon server-dev-proxied.js"
  },
  "devDependencies": {
    "@fastify/http-proxy": "^8.4.0",
    "fastify": "^4.11.0"
  }
}
